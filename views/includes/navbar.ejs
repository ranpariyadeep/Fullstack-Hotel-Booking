<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings">
      <i class="fa-regular fa-compass"></i
    ></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <!-- <div class="search-bar ms-auto">
        <div class="filter where">
          <label>Where</label>
          <input type="text" placeholder="Search destinations" />
        </div> -->


        <!-- <div class="divider"></div> -->

        <!-- <div class="filter when position-relative" id="booking-form">
  <label>When</label>
  <input type="text" id="when" placeholder="Any Weekend" readonly/>

  <div id="calendar-popup" class="calendar-popup shadow-sm">
    <div class="d-flex gap-2">
      <div id="checkin-inline"></div>
      <div id="checkout-inline"></div>
    </div>
    <button type="button" id="close-popup" class="btn btn-sm btn-outline-secondary mt-2">Close</button>
  </div>
</div> -->


        <!-- <button class="search-btn"><i class="icon">🔍</i> Search</button>
      </div> -->

 <!-- <script>
const whenInput = document.getElementById("when");
const popup = document.getElementById("calendar-popup");
const closeBtn = document.getElementById("close-popup");

let checkinDate = null;
let checkoutDate = null;

// Check if date is in range
function isInRange(date) {
  if (checkinDate && checkoutDate) {
    const d = new Date(date);
    const start = new Date(checkinDate);
    const end = new Date(checkoutDate);
    return d > start && d < end;
  }
  return false;
}

// Update input field
function updateWhenInput() {
  if (checkinDate && checkoutDate) {
    whenInput.value = checkinDate + " → " + checkoutDate;
  } else if (checkinDate) {
    whenInput.value = checkinDate;
  }
}

// Style the day elements
function styleDays(dayElem, date) {
  if (checkinDate && date.toDateString() === new Date(checkinDate).toDateString()) dayElem.classList.add("selected");
  if (checkoutDate && date.toDateString() === new Date(checkoutDate).toDateString()) dayElem.classList.add("selected");
  if (isInRange(date)) dayElem.classList.add("inRange");
  if (date < new Date().setHours(0,0,0,0)) dayElem.classList.add("disabled");
}

// Check-in calendar
const checkinCal = flatpickr("#checkin-inline", {
  inline: true,
  minDate: "today",
  onDayCreate: function(dObj, dStr, fp, dayElem) {
    styleDays(dayElem, dayElem.dateObj);
  },
  onChange: function(selectedDates, dateStr) {
    checkinDate = dateStr;
    checkoutCal.set('minDate', dateStr);
    // Force checkout to next month
    const nextMonth = selectedDates[0].getMonth() + 1;
    const nextYear = selectedDates[0].getFullYear() + (nextMonth > 11 ? 1 : 0);
    checkoutCal.changeMonth(nextMonth > 11 ? 0 : nextMonth, true);
    checkoutCal.changeYear(nextYear);
    checkinCal.redraw();
    checkoutCal.redraw();
    updateWhenInput();
  }
});

// Check-out calendar
const checkoutCal = flatpickr("#checkout-inline", {
  inline: true,
  minDate: "today",
  onDayCreate: function(dObj, dStr, fp, dayElem) {
    styleDays(dayElem, dayElem.dateObj);
  },
  onChange: function(selectedDates, dateStr) {
    checkoutDate = dateStr;
    checkinCal.redraw();
    checkoutCal.redraw();
    updateWhenInput();
  }
});

// Show popup
whenInput.addEventListener("click", () => popup.style.display = "block");

// Close popup
closeBtn.addEventListener("click", () => popup.style.display = "none");

// Click outside to close
document.addEventListener("click", (e) => {
  if (!popup.contains(e.target) && e.target !== whenInput) popup.style.display = "none";
});
</script> -->



      

      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Become a host</a>
        <% if(!currentUser) {%>
        <a class="nav-link" href="/signup"><b>Sing Up</b></a>
        <a class="nav-link" href="/login"><b>Log in</b></a>
        <% } %> <% if(currentUser) {%>
        <a class="nav-link" href="/logout"><b>Log out</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
