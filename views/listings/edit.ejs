<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
</head>
<body>
    
    <h3>Edit Your Listing</h3>
    <form action="/listings/<%= listing._id%>?_method=PUT" method="POST">
        <input  name="listing[title]" id="" value="<%= listing.title%>" type="text">
        <br>
        <textarea name="listing[description]"  type="text" id=""> <%= listing.description%>></textarea>
        <br>
        <input  name="listing[image]" id="" type="text" value="<%= listing.image%> "">
        <br>
        <input  name="listing[price]" id="" value="<%= listing.price%>" type="number">
        <br>
        <input  name="listing[location]" id="" value="<%= listing.location%>" type="text">
        <br>
        <input  name="listing[country]" id="" value="<%= listing.country%>" type="text">
        <br>
        <button>Submit</button>
    </form>
</body>
</html>